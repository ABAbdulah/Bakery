import React from "react";
import ReactDOM from "react-dom/client";
//import ContactForm from "./component/ContactUs.js";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    // <ContactForm />
  </React.StrictMode>
);
